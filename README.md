# validation
The validation application checks the form in real time whenever the user presses each key on the keyboard when the form is filled out.
There are 7 form elements in the form, "username", "name", "surname", "e-mail address", "Password" "Password reset", "about".
